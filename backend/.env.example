# MongoDB connection string (without real credentials)
DATABASE_URL=mongodb+srv://<username>:<password>@<cluster-url>

# JWT / app secret
SECRET_KEY=replace-with-a-strong-secret

# Server port
PORT=3000
